<section class="vgallery">
  <div class="container">
    <div class="row">
        <% @vendor.each do |v| %>
          <div class="col-sm-4">
            <%= image_tag v.logo.url(:thumb), class: 'img-responsive' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</section>
